// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // User site served at the domain root (https://ardhra7ms.github.io/).
  site: 'https://ardhra7ms.github.io',
});
